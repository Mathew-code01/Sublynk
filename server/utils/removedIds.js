// server/utils/removedIds.js
// server/utils/removedIds.js

function isIdRegisteredRemoved(id) {
  return false; // Placeholder, always allows all subtitles
}

module.exports = {
  isIdRegisteredRemoved,
};
